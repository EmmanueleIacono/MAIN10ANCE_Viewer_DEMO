{
  "name": "main10ance_viewer_app",
  "version": "1.0.0",
  "description": "Applicazione MAIN10ANCE per la manutenzione programmata",
  "main": "start.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node start.js",
    "server": "nodemon start.js --ignore client/",
    "build": "npm install && cd client && npm install && npm run build",
    "typecheck": "node --check start.js && npm run typecheck --prefix client"
  },
  "author": "Emmanuele Iacono",
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1",
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-fileupload": "^1.5.2",
    "forge-apis": "^0.9.12",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
